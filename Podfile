# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ceiba-ios-test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Alamofire', '~> 4.9'

  # Pods for ceiba-ios-test

  target 'ceiba-ios-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ceiba-ios-testUITests' do
    # Pods for testing
  end

end
